import {render} from "@testing-library/react"
describe('MangaImage Component', () => {
  it('should in ideal case show image on passed imageLink props',() => {
     const {} = render
  })
})

export {}
